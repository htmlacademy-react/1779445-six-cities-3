export const generateOfferRatingStars = (currentRating: number) => {
  const rating = currentRating.toFixed();
  switch (rating) {
    case '1':

  }
  return (
    <span style={{width: '' }}></span>
  );
};
